<%= form_with(model: effect, local: true) do |form| %>
  <% if effect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(effect.errors.count, "error") %> prohibited this effect from being saved:</h2>

      <ul>
      <% effect.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :effect %>
    <%= form.text_field :effect %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
